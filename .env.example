# Cognitude MVP - Environment Variables

# Database Configuration
DATABASE_URL=postgresql+psycopg2://myuser:mypassword@db/mydatabase

# JWT Secret (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here

# Email Configuration (Optional - for drift alert emails)
# Gmail Example:
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=alerts@cognitude.io
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# SendGrid Example:
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# FROM_EMAIL=alerts@yourdomain.com
# SMTP_SERVER=smtp.sendgrid.net
# SMTP_PORT=587

# AWS SES Example:
# SMTP_USERNAME=your-ses-username
# SMTP_PASSWORD=your-ses-password
# FROM_EMAIL=alerts@yourdomain.com
# SMTP_SERVER=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587

# Scheduler Configuration (Optional)
# DRIFT_CHECK_INTERVAL_MINUTES=15  # How often to run drift detection

# Logging (Optional)
# LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
